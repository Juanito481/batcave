version: "3.8"
services:
  batcave-server:
    build:
      context: ..
      dockerfile: server/Dockerfile
    ports:
      - "7777:7777"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
